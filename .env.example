# Optional API keys / credentials (never commit .env)
# Used for Google Places enrichment (optional)
GOOGLE_PLACES_API_KEY=

# If you later automate login (not required in this tool),
# you can provide credentials via env. Currently login is manual.
INSTAGRAM_USERNAME=
INSTAGRAM_PASSWORD=

